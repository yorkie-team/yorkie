{
  "name": "@wafflebase/agent-scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Autonomous-pipeline agent scripts (review panel orchestrator). Not part of the workspace build.",
  "scripts": {
    "test": "node --test-timeout=60000 --test '**/*.test.mjs'"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.217",
    "zod": "4.4.3"
  },
  "comment": "This project has its own npm lockfile and sits outside the pnpm workspace, so the root package.json's pnpm.overrides do not reach it. Transitive security pins are duplicated here on purpose.",
  "overrides": {
    "fast-uri": "^3.1.6",
    "qs": "^6.16.0"
  }
}
