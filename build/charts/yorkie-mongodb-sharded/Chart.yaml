apiVersion: v2
name: yorkie-mongodb-sharded
description: yorkie-mongodb-sharded provides a sharded MongoDB cluster for yorkie server

type: application

version: 0.4.13

appVersion: "0.4.13"

dependencies:
  - name: mongodb-sharded
    version: "7.2.2"
    repository: "https://charts.bitnami.com/bitnami"
